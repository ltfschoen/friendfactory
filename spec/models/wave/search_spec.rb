require 'spec_helper'

describe Wave::Search do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    pending
    Wave::Search.create!(@valid_attributes)
  end
end
