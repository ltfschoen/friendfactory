require 'spec_helper'

describe "Admin::Tags" do
  describe "GET /admin_tags" do
    it "works! (now write some real specs)" do
      pending
      visit admin_tags_path
      response.status.should be(200)
    end
  end
end
