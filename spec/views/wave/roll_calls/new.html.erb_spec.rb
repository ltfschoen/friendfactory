require 'spec_helper'

describe "wave_polaroids/new.html.erb" do
  before(:each) do
    # assign(:polaroid, stub_model(Wave::Polaroid).as_new_record)
  end

  it "renders new polaroid form" do
    # render
    # rendered.should have_selector("form", :action => wave_polaroids_path, :method => "post") do |form|
    # end
  end
end
