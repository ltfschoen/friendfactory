require 'spec_helper'

describe "admin_invitation_personals/show.html.erb" do
  before(:each) do
    # @personal = assign(:personal, stub_model(Admin::Invitation::Personal))
  end

  it "renders attributes in <p>" do
    pending
    render
  end
end
