.posting[comment]
  %table
    %tr
      %td.avatar
        = avatar_image_tag(comment.user.profile.avatar, :style => :thumb) rescue nil
      %td
        %div
          = link_to_profile(comment.user) if comment.user
          = distance_of_time_in_words_to_now(comment.created_at)
        %p.body= comment.body