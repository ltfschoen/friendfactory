- stylesheet 'admin'
- javascript 'admin'
.grid_14.push_1.admin.canvas.site
  %h1= "#{@site.name}"
  = form_for @site, :url => admin_site_path, :html => { :method => :put } do |f|  
    = render :partial => 'form', :locals => { :f => f }
