.wave.header
  %h1 User Info Tag Scrubber
.grid_3
  &nbsp;
.grid_10
  .admin
    %table
      %tr
        %th Defective
        %th Corrected
        %th
      - @tags.each do |tag|
        %tr
          %td= tag.defective
          %td= tag.corrected
          %td.actions
            = link_to 'Edit', edit_admin_tag_path(tag)
            = link_to 'Delete', tag, :confirm => 'Are you sure?', :method => :delete    
    .button-bar
      = link_to 'Add Scrubbed Tag', new_admin_tag_path
      |
      = link_to 'Commit', commit_admin_tags_path

.grid_3
  &nbsp;