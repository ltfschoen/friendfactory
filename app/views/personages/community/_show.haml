= render :partial => File.join('personages', 'shared', 'avatar')
= render :partial => File.join('personages', 'community', 'biometrics')
= render :partial => File.join('personages', 'shared', 'preferences')
