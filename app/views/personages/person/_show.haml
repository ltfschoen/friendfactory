= render :partial => File.join('personages', 'shared', 'avatar'),:locals => { :personage => personage }
= render :partial => File.join('personages', 'person', 'show_biometrics'), :locals => { :personage => personage }
= render :partial => File.join('personages', 'person', 'show_map'), :locals => { :personage => personage }
=# render :partial => File.join('personages', 'person', 'show_invitations'), :locals => { :personage => personage }
= render :partial => File.join('personages', 'shared', 'preferences'), :locals => { :personage => personage }
= render :partial => File.join('personages', 'shared', 'show_credentials'), :locals => { :personage => personage }
