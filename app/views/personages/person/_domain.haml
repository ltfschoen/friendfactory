%tr{ :class => domain.name }
  %td.label
    = domain.display_name
  %td.detail
    = domain.values.where(:id => persona.biometric_value_id(domain)).limit(1).first.try(:display_name) || '&nbsp;'.html_safe
