.headshot-container
  .headshot
    .face
      .pane
        = render :partial => File.join('personages', 'person', 'biometrics'), :locals => { :persona => personage.persona }
      .button-bar
        = link_to 'Change', edit_profile_path(personage, :pane => 'biometrics'), :class => 'button edit_profile'
