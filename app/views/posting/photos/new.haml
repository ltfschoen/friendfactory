- photo ||= Posting::Photo.new(:user_id => current_user.id)
= form_for photo, :url => wave_posting_photos_path(@wave), :html => { :multipart => true }, :remote => true do |f|
  %table
    %tr
      %td.avatar.thumb
        .post_it.form<
          = avatar_image_tag(current_user, :style => :thumb, :align => 'left', :anonymous => false, :gender => current_user.user_info.gender)
          %span= "Posted by #{link_to_profile(current_user)}".html_safe
          = f.text_area :subject, :placeholder => 'Caption'
      %td
        #posting_photo_upload_well
          = image_tag 'silhouette-q.gif', :class => 'photo h4x6 small silhouette', :site => false
        .button-bar
          = f.file_field :image
          = f.button 'Cancel', :id => 'posting_photo_cancel'
