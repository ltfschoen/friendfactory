.posting[comment]<  
  .comment.canvas
    .body
      .content
        = avatar_image_tag(comment.user.avatar, :style => :thimble, :anonymous => false)
        = link_to_profile(comment.user)
        = comment.body
    .footer<
      &nbsp;
