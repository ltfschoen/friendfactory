- if current_user
  .posting_comment.form.hidden
    .body
      = form_for Posting::Comment.new, :url => posting_comments_path(posting), :html => { :id => "posting_#{posting.id}_new_comment" }, :remote => true do |f|
        = f.text_area :body
        .button-bar
          = f.submit 'OK'
          = f.button 'Cancel', :class => 'cancel'
    .footer
      &nbsp;
