- if posting.present? && posting.unpublished?
  :plain
    var $posting = $("##{dom_id(posting)}").closest('.posting');
    $posting.fadeTo('fast', 0.0, function() {
      $posting.slideUp('fast', 'linear', function() {
        $posting.remove();
      });
    });
