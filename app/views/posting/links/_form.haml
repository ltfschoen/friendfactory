= form_for Posting::Link.new, :url => wave_posting_links_path(@wave), :remote => true do |f|
  .button-bar
    = f.text_field :url, :placeholder => 'http:// to something to share'
    = f.submit 'OK'
    = f.button 'Cancel', :class => 'cancel'
