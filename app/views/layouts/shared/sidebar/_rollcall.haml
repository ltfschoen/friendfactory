.block.rollcall
  - users.each_with_index do |user, idx|
    - css_class = (idx + 1) % 5 == 0 ? 'omega' : nil
    .portrait{ :class => css_class }= current_page_for_wave?(user.profile) ? monochrome_thimble_image_tag(user) : thimble_link_to_profile(user)
  = link_to "More #{persona_type.to_s.pluralize.titleize} &rarr;".html_safe, rollcall_path, :class => 'more'
