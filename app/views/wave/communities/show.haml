= render :partial => 'rollcall', :locals => { :profiles => @profiles }
= render :partial => 'layouts/shared/nav'
- @postings.each do |posting|
  = render_community_posting(posting)
=# render :partial => 'placeholders.x'
=# will_paginate @postings, :class => 'pagination append-top'
= render :partial => 'layouts/shared/unpublish_overlay'
