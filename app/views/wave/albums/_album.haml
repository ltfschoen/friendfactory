- user = album.user
.post.photos[album]
  .remove{:title => "Remove this post"} ×
  .portrait
    = thimble_link_to_profile(user)
  .clear
    .photo
      = image_tag(album.photos.limit(1).first.image.url(:h4x6))
    %span.date Posted #{distance_of_time_in_words_to_now(proxy.created_at)} ago
    %span.comments
      = link_to (pluralize(album.postings.published.count, 'comment') + '&nbsp;&rarr;').html_safe, posting_comments_path(proxy), :remote => true
.reaction
